[pytest]
# Additional pytest configuration (pytest.ini format)
# Some settings work better in ini format than pyproject.toml

# Base URL for Playwright tests (set dynamically by conftest.py)
# base_url = http://localhost:8000

# Playwright-specific settings
# Browsers are specified via command line: --browser chromium --browser firefox --browser webkit

# Screenshot settings for visual regression
# --update-snapshots flag regenerates baseline images

# Output directory for test artifacts
# test-results/

# HTML report output
# --html=test-results/report.html --self-contained-html
